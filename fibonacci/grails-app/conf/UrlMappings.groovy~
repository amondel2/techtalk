class UrlMappings {

	static mappings = {
        "/$controller/$action?/$id?(.$format)?"{
            constraints {
                // apply constraints here
            }
        }

        "fibonacci/$fibNumber"(controller:"Fibonacci",action:"index")

        "/"(view:"/index")
        "500"(view:'/error')
	}
}
